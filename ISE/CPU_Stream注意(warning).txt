(nop:111110,剩余全为0)(空语句)
(stop:111111,剩余全为0)(停止语句)

branch后加两条nop

j/jal/jr后加一条nop
j型指令记得只表示[3:28]位(从1开始计数)

sw前加两条nop
lw后加两条nop

如果$rs和$rt的地址相同,且前一条或前两条为addi,addi赋值地址与$rs相同,则在ADD前增加2或1条nop指令(与前对应)

$0不能做$rs,只能做$rt


rom存的指令,ram存的数据(当成闪存).

